apiVersion: v1
kind: Service
metadata:
  name: {{ include "todolist-frontend.fullname" . }}
  labels:
    {{- include "todolist-frontend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "todolist-frontend.selectorLabels" . | nindent 4 }}
