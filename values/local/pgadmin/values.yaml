pgadmin4:
  ingress:
    enabled: true
    ingressClassName: nginx
    annotations:
      cert-manager.io/cluster-issuer: "ca-clusterissuer"
    hosts:
      - host: pgadmin-172-19-0-3.nip.io
        paths:
          - path: /
            pathType: Prefix
