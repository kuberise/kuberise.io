replicaCount: 3

ingress:
  annotations:
      cert-manager.io/cluster-issuer: "ca-clusterissuer-external"
  tls:
  - hosts:
    - "*.minikube.kuberise.dev"
    secretName: wildcard-cert-tls
  enabled: true
  hosts:
    - host: frontend.minikube.kuberise.dev
      paths:
        - path: /
          pathType: ImplementationSpecific
