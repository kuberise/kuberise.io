replicaCount: 2

ingress:
  enabled: true
  annotations:
      cert-manager.io/cluster-issuer: "ca-clusterissuer-external"
  tls:
  - hosts:
    - "*.minikube.kuberise.dev"
    secretName: wildcard-cert-tls
  hosts:
    - host: frontend-tst.minikube.kuberise.dev
      paths:
        - path: /
          pathType: ImplementationSpecific
