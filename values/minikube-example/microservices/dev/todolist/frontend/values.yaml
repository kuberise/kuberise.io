replicaCount: 1

ingress:
  enabled: true
  annotations:
      cert-manager.io/cluster-issuer: "ca-clusterissuer-external"
  tls:
  - hosts:
    - "*.minikube.kuberise.dev"
    secretName: wildcard-cert-tls
  hosts:
    - host: frontend-dev.minikube.kuberise.dev
      paths:
        - path: /
          pathType: ImplementationSpecific
