selfSignedCertificate:
  enabled: true # used for internal applications that need to be accessed via HTTPS
  createCaCertificate: false

letsencryptProductionCertificate:
  enabled: true

# TODO: try staging certificate

subdomains:
  - "argocd"
  - "pgadmin"
  - "show-env"
  - "keycloak"
  - "grafana"
  - "prometheus"
  - "frontend-https"
