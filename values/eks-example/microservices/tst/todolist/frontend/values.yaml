replicaCount: 2

ingress:
  enabled: true
  className: external-nginx
  annotations:
      cert-manager.io/cluster-issuer: http-01-production
  tls:
  - hosts:
    - "frontend-tst.eks.kuberise.dev"
    secretName: wildcard-cert-tls
  hosts:
    - host: frontend-tst.eks.kuberise.dev
      paths:
        - path: /
          pathType: ImplementationSpecific
