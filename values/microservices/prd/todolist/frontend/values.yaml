replicaCount: 3

ingress:
  tls:
  - hosts:
    - "*.127.0.0.1.nip.io"
    secretName: wildcard-cert-tls
  enabled: true
  hosts:
    - host: frontend.127.0.0.1.nip.io
      paths:
        - path: /
          pathType: ImplementationSpecific
