applications:
  cnpg-database:
    enabled: false

helmCharts:
  keycloak:
    enabled: false

  ingress-nginx:
    enabled: false # deployed by the terraform code that created the eks cluster

  kube-prometheus-stack:
    enabled: false

  loki:
    enabled: false

  promtail:
    enabled: false

  cloudnative-pg:
    enabled: false

  todolist-frontend-dev:
    enabled: false

  todolist-frontend-tst:
    enabled: false

  todolist-frontend-prd:
    enabled: false

  show-env-dev:
    enabled: true
    path: apps/show-env
    values: eks-example/microservices/dev/show-env/values.yaml

  cert-manager:
    enabled: false

  backstage:
    enabled: false

  aws-lb-controller:
    enabled: false # deployed by the terraform code that created the eks cluster
